# Example environment configuration for Looking Glass

# Server port (default: 3000)
PORT=3000

# Database path (default: ./leases.db)
# For production, use a persistent volume mount
DB_PATH=/data/leases.db

# Kea DHCP endpoints (JSON array)
# Single endpoint:
ENDPOINTS='[{"name":"default","url":"http://127.0.0.1:8000"}]'

# Multiple endpoints:
# ENDPOINTS='[{"name":"128","url":"http://localhost:8000"},{"name":"129","url":"http://localhost:8001"}]'

# Kubernetes context (optional, for kubectl exec mode)
# KUBE_CONTEXT=my-cluster
